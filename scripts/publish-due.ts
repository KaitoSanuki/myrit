import { publishDuePosts } from "@/lib/jobs/publish";

publishDuePosts()
  .then((result) => {
    console.log(JSON.stringify(result, null, 2));
  })
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
